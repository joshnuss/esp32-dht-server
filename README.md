An HTTP-based temperature sensor using an ESP32 Super Mini and a DHT22 sensor.

## Setup environment

Add SSID & password to `settings.py`:

```python
WLAN_SSID = "<ssid>"
WLAN_PASSWORD = "<password>"
```

## Installation

```sh
mpremote cp *.py :
```

## Running

```sh
mpremote run main.py
```
